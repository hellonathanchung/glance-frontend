import React from "react";

const TransactionCard = (props) => {
  debugger;
  const { gas_price } = props.transactionAttributes;
  return <div className="transaction-card"> Hello</div>;
};

export default TransactionCard;
